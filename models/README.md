Put all YOLO model files in this folder.

Supported file types:
- `.pt`
- `.onnx`
- `.engine`
- `.torchscript`

The project generates `model_index.json` here so it can map object classes to the right model.
